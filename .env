DATABASE_URL=file:/home/z/my-project/db/custom.db

# Google OAuth Client ID for Drive sync (optional — user provides their own).
# Create one at https://console.cloud.google.com/apis/credentials
# Enable "Google Drive API" + "Google Identity Services" for the project.
# Authorized JavaScript origins: <your-app-origin>
GOOGLE_CLIENT_ID=""
